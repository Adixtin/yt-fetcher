Its a python app that fetches videos from yt using the yt-data api amd downloads them using yt_dlp
U need to get the yt-data api from google cloud -> https://developers.google.com/youtube/v3 and put it in .env
```YOUTUBE_API_KEY="your_key"```
